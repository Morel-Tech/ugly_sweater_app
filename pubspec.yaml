name: ugly_sweater_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  blurhash: ^1.0.0
  camera: ^0.9.4+5
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.3
  flutter_svg: ^1.0.0
  loading_bloc_builder: ^0.0.1-dev.2
  supabase_flutter: ^0.2.9
  uuid: ^3.0.5
  octo_image: ^1.0.1
  swipe_cards: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/
